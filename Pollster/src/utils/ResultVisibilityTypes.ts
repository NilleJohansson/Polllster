const ALWAYS_PUBLIC = "Always public";
const PUBLIC_AFTER_END_DATE = "Public after end date";
const PUBLIC_AFTER_VOTE = "Public after vote";
const NOT_PUBLIC = "Not public";

const resultVisibilityTypeList: Array<string> = [
    ALWAYS_PUBLIC,
    PUBLIC_AFTER_END_DATE,
    PUBLIC_AFTER_VOTE,
    NOT_PUBLIC
]

export { ALWAYS_PUBLIC, PUBLIC_AFTER_END_DATE, PUBLIC_AFTER_VOTE, NOT_PUBLIC, resultVisibilityTypeList }